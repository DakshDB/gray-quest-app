import 'package:flutter/material.dart';

// main theme colors
class AppTheme {
  static final Color backgroundColor = Color(0xFF000000);
  static final Color primaryColor = Color(0xFF6FB856);
  static final Color accentColor = Color(0xFF1A1A1A);
  static final Color cardColor = Color(0xFF424242);
  static final Color secondaryHeaderColor = Color(0xFFADADAD);
  static final Color textColor = Color(0xFFFFFFFF);
  static final cardShape = RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(10.0));
  static final shape = RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(20.0));
}